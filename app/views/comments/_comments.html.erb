<% @blog_post.comments.each do |comment| %>

    <h4>by <%= link_to comment.user.username, user_session_path(id: comment.user.id) %></h4>
    <p><%= time_ago_in_words(comment.created_at) %> ago</p>
    <p><%= comment.comment_entry %></p>

<% end %>